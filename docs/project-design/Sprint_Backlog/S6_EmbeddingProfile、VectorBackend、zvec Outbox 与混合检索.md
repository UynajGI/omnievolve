# S6 Sprint Backlog：EmbeddingProfile、VectorBackend、zvec Outbox 与混合检索

> Phase：Phase 2  
> 推荐周期：6 个工作日  
> 阶段门：M3-B：Retrieval Foundation Ready  
> 设计追溯：第二章向量、第三章 storage vector、第四章 4.2、第八章

## Sprint Goal

建立可迁移、可修复、作用域安全的思想/代码向量与 FTS5 混合检索。

## 进入条件（Definition of Ready）

- 上游关键接口已冻结并有版本号。
- 所有 A 级任务明确 owner 和审查人。
- 依赖、测试夹具、目标平台和配置已准备。
- 不存在未处理的 P0 设计分歧。

## 任务卡

### S6-01 · 冻结 EmbeddingProfile 数据模型

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-10
- **参考工作量**：6h
- **可并行**：否
- **交付物**：provider/model/revision/dim/normalization/input_type
- **验收条件**：同一 profile 可复现；维度不再全局写死
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-02 · 实现 Embedder Protocol 与 fake embedder

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S6-01
- **参考工作量**：5h
- **可并行**：可
- **交付物**：utils/embedding.py
- **验收条件**：批量、超时、错误和维度校验统一
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-03 · 实现 API 文本/代码 Embedder Adapter

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S6-02
- **参考工作量**：7h
- **可并行**：可
- **交付物**：provider adapters
- **验收条件**：code/thought 使用独立 profile；调用计量
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-04 · 实现本地 sentence-transformers Adapter

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S6-02
- **参考工作量**：6h
- **可并行**：可
- **交付物**：local embedder
- **验收条件**：模型缺失时诊断清晰；禁止静默下载或提供显式策略
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-05 · 冻结 VectorBackend Protocol

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-01
- **参考工作量**：5h
- **可并行**：可
- **交付物**：create/upsert/query/delete/healthcheck
- **验收条件**：业务层不依赖 zvec 具体 API
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-06 · 实现 NumPy 精确检索 fallback

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S6-05
- **参考工作量**：6h
- **可并行**：可
- **交付物**：vector_numpy.py
- **验收条件**：小规模可用；结果可作为 zvec 对照 oracle
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-07 · 实现 zvec Adapter 与 collection lifecycle

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-05
- **参考工作量**：8h
- **可并行**：否
- **交付物**：zvec_backend.py
- **验收条件**：按官方 API；单写者约束；profile 隔离目录
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-08 · 实现 vector_index_outbox 生产端

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S1-11,S4-01
- **参考工作量**：5h
- **可并行**：可
- **交付物**：outbox publisher
- **验收条件**：Candidate/Memory 提交与 outbox 同事务
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-09 · 实现 Outbox Indexer 与幂等消费

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-07,S6-08
- **参考工作量**：8h
- **可并行**：否
- **交付物**：vector_indexer.py
- **验收条件**：崩溃重试不重复；成功后状态原子更新
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-10 · 实现索引修复与 reconcile

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-09
- **参考工作量**：7h
- **可并行**：可
- **交付物**：repair command
- **验收条件**：SQLite 有/向量无、向量有/SQLite 无均可诊断修复
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-11 · 实现 FTS5 文档与作用域索引

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S1-12
- **参考工作量**：6h
- **可并行**：可
- **交付物**：lexical index
- **验收条件**：experiment/task/domain/memory level 可过滤
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-12 · 实现 Hybrid Retriever 与融合排序

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-06,S6-07,S6-11
- **参考工作量**：8h
- **可并行**：否
- **交付物**：hybrid_retriever.py
- **验收条件**：向量+BM25 结果归一化；权重可配置；返回可解释命中
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-13 · 实现 code/thought 独立索引与元数据过滤

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S6-12
- **参考工作量**：5h
- **可并行**：可
- **交付物**：retrieval filters
- **验收条件**：不混维度、不跨 profile、不跨 scope
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-14 · 实现 profile 迁移/重建流程

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-09,S6-10
- **参考工作量**：7h
- **可并行**：可
- **交付物**：embedding migrate command
- **验收条件**：新旧索引并存；切换可回滚；旧结果仍可解释
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-15 · 编写 zvec vs NumPy 一致性测试

- **Owner**：测试与质量
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S6-06,S6-07
- **参考工作量**：6h
- **可并行**：可
- **交付物**：retrieval oracle tests
- **验收条件**：固定数据 top-k 在容差内一致
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-16 · 编写 outbox 崩溃恢复测试

- **Owner**：测试与质量
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S6-09,S6-10
- **参考工作量**：7h
- **可并行**：可
- **交付物**：fault tests
- **验收条件**：在索引前/后中断均能最终一致
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S6-17 · 编写向量配置与迁移文档

- **Owner**：文档与发布
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S6-01~S6-16
- **参考工作量**：4h
- **可并行**：可
- **交付物**：docs/vector_storage.md
- **验收条件**：说明 profile、索引目录、重建、备份和限制
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。


## Sprint Definition of Done

- [ ] 所有关键路径任务 Done；非关键任务若延期必须更新 scope 与风险。
- [ ] 新增公共接口有 contract tests 和文档。
- [ ] 数据/状态变更有迁移或向后兼容策略。
- [ ] P0/P1 缺陷已关闭或在阶段门由用户接受。
- [ ] 交付物可从干净环境复现。
- [ ] 更新 `03_WBS总表.md` 状态、`07_风险登记册.md` 和 `10_需求追溯矩阵.md`。

## Sprint Review 重点

1. 是否真正满足设计边界，而非仅有演示路径。
2. 是否引入新的隐式全局状态、越权执行或双重真相源。
3. 是否可以故障恢复、重放和审计。
4. 是否需要砍 scope，而不是把风险推迟到发布前。
