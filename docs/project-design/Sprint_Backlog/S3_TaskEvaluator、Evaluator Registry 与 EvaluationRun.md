# S3 Sprint Backlog：TaskEvaluator、Evaluator Registry 与 EvaluationRun

> Phase：Phase 1  
> 推荐周期：4 个工作日  
> 阶段门：M2-A：Reproducible Evaluation Ready  
> 设计追溯：第一章评估主权、第五章 5.1/5.1.1、第六章 Fast Loop

## Sprint Goal

统一“评估器声明计划、Sandbox 执行、评估器解析结果”的边界，并保证评估语义可版本化、不可被元智能体篡改。

## 进入条件（Definition of Ready）

- 上游关键接口已冻结并有版本号。
- 所有 A 级任务明确 owner 和审查人。
- 依赖、测试夹具、目标平台和配置已准备。
- 不存在未处理的 P0 设计分歧。

## 任务卡

### S3-01 · 冻结 EvaluationPlan/EvalOutput/EvaluationContext

- **Owner**：评估与实验
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S2-01
- **参考工作量**：6h
- **可并行**：否
- **交付物**：eval/task_evaluator.py 数据类
- **验收条件**：执行命令、挂载、阶段、输出约定和解析边界完整
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-02 · 实现 TaskEvaluator Protocol

- **Owner**：评估与实验
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S3-01
- **参考工作量**：4h
- **可并行**：可
- **交付物**：build_plan/parse_result/get_baseline
- **验收条件**：评估器无法直接获得宿主 subprocess 能力
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-03 · 实现 Evaluator Registry 与版本 digest

- **Owner**：评估与实验
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-10,S3-01
- **参考工作量**：7h
- **可并行**：否
- **交付物**：eval/evaluator_registry.py
- **验收条件**：代码、配置、数据集 hash、评分规则可生成不可变版本
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-04 · 实现任务语义不可变策略

- **Owner**：评估与实验
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S3-03
- **参考工作量**：6h
- **可并行**：可
- **交付物**：semantic lock
- **验收条件**：Task semantics、correctness、hidden set、score aggregation 被锁定
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-05 · 实现 EvaluationPlan 校验器

- **Owner**：安全与沙箱
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S3-01,S2-09
- **参考工作量**：5h
- **可并行**：可
- **交付物**：plan validator
- **验收条件**：非法命令、挂载、网络和环境变量请求被拒绝
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-06 · 实现 EvaluationRun 状态机

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-10,S3-01
- **参考工作量**：7h
- **可并行**：否
- **交付物**：pending→leased→running→parsed→committed/failed
- **验收条件**：重复回调与异常恢复不会生成双结果
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-07 · 实现随机种子、重复次数与统计字段

- **Owner**：评估与实验
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S3-06
- **参考工作量**：5h
- **可并行**：可
- **交付物**：seed/repetition model
- **验收条件**：同一候选可多次评估；均值/方差/置信区间可记录
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-08 · 实现正确性门与性能评分解耦

- **Owner**：评估与实验
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S3-02
- **参考工作量**：5h
- **可并行**：可
- **交付物**：score gate
- **验收条件**：未通过 correctness 的候选不能靠性能分进入 elite
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-09 · 实现 Progressive Evaluation 阶段描述

- **Owner**：评估与实验
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S3-01,S3-05
- **参考工作量**：5h
- **可并行**：可
- **交付物**：small→full plan
- **验收条件**：阶段切换不改变评分语义；只改变成本与资源分配
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-10 · 实现 Python demo evaluator

- **Owner**：评估与实验
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S3-02,S3-05
- **参考工作量**：6h
- **可并行**：可
- **交付物**：examples/python_optimization
- **验收条件**：可从初始代码跑 correctness+benchmark
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-11 · 实现 baseline 登记与重跑

- **Owner**：评估与实验
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S3-03,S3-06
- **参考工作量**：5h
- **可并行**：可
- **交付物**：baseline service
- **验收条件**：环境变更后可自动重跑 baseline 并对比
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-12 · 实现解析失败与异常分类

- **Owner**：评估与实验
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S3-02,S3-06
- **参考工作量**：4h
- **可并行**：可
- **交付物**：failure taxonomy
- **验收条件**：build/timeout/runtime/parse/wrong-answer 分离
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-13 · 实现 evaluator 不可越权测试

- **Owner**：测试与质量
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S3-04,S3-05
- **参考工作量**：7h
- **可并行**：可
- **交付物**：security/semantic tests
- **验收条件**：尝试修改隐藏数据、评分文件、挂载策略均失败
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-14 · 实现 EvaluationRun 复现测试

- **Owner**：测试与质量
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S3-06,S3-07,S3-10
- **参考工作量**：7h
- **可并行**：可
- **交付物**：reproducibility tests
- **验收条件**：给定 artifact/evaluator/environment/seed 可重放
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S3-15 · 编写评估器开发指南

- **Owner**：文档与发布
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S3-01~S3-14
- **参考工作量**：4h
- **可并行**：可
- **交付物**：docs/evaluator_authoring.md
- **验收条件**：含安全边界、版本策略、示例与反例
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。


## Sprint Definition of Done

- [ ] 所有关键路径任务 Done；非关键任务若延期必须更新 scope 与风险。
- [ ] 新增公共接口有 contract tests 和文档。
- [ ] 数据/状态变更有迁移或向后兼容策略。
- [ ] P0/P1 缺陷已关闭或在阶段门由用户接受。
- [ ] 交付物可从干净环境复现。
- [ ] 更新 `03_WBS总表.md` 状态、`07_风险登记册.md` 和 `10_需求追溯矩阵.md`。

## Sprint Review 重点

1. 是否真正满足设计边界，而非仅有演示路径。
2. 是否引入新的隐式全局状态、越权执行或双重真相源。
3. 是否可以故障恢复、重放和审计。
4. 是否需要砍 scope，而不是把风险推迟到发布前。
