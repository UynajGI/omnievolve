# S4 Sprint Backlog：Candidate 图、基础 Scheduler、Job Lease 与恢复

> Phase：Phase 1  
> 推荐周期：7 个工作日  
> 阶段门：M2-B：Trusted Evolution Core Ready  
> 设计追溯：第三章 engine、第四章 Candidate/Lineage/SearchState、第五章 5.4、第六章 6.1、第七章

## Sprint Goal

打通可恢复的候选进化最小闭环，并满足 500 候选稳定运行和 kill -9 恢复验收。

## 进入条件（Definition of Ready）

- 上游关键接口已冻结并有版本号。
- 所有 A 级任务明确 owner 和审查人。
- 依赖、测试夹具、目标平台和配置已准备。
- 不存在未处理的 P0 设计分歧。

## 任务卡

### S4-01 · 实现 Candidate/Thought Repository

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S1-09,S1-10
- **参考工作量**：6h
- **可并行**：可
- **交付物**：candidate_store.py
- **验收条件**：候选与思想分离；状态转换受约束
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-02 · 实现多父代 CandidateLineage

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-10
- **参考工作量**：6h
- **可并行**：可
- **交付物**：lineage repository
- **验收条件**：mutation/crossover 多父代顺序和关系类型可记录
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-03 · 实现 reference edge 与 lineage edge 分离

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S4-02
- **参考工作量**：4h
- **可并行**：可
- **交付物**：reference_edge repository
- **验收条件**：引用其他分支不伪装成遗传父代
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-04 · 实现 SearchState 最小字段与更新规则

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-10,S4-01
- **参考工作量**：7h
- **可并行**：否
- **交付物**：visit/value/frontier/island state
- **验收条件**：并发更新不丢增量；字段语义有单测
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-05 · 实现 Job Lease/Heartbeat/Expiry

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-11
- **参考工作量**：8h
- **可并行**：否
- **交付物**：storage/job_store.py
- **验收条件**：任务可认领、续租、过期回收；双 worker 不重复执行
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-06 · 实现幂等键与结果提交协议

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S3-06,S4-05
- **参考工作量**：7h
- **可并行**：否
- **交付物**：idempotent commit
- **验收条件**：同一 candidate+evaluator+environment+seed 仅有一个 committed run
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-07 · 实现基础 Scheduler 队列

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S4-05,S4-06
- **参考工作量**：8h
- **可并行**：否
- **交付物**：engine/scheduler.py
- **验收条件**：可按优先级投递生成、执行、解析任务
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-08 · 实现候选 Artifact materialize 与 diff apply

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S1-06,S4-01
- **参考工作量**：6h
- **可并行**：可
- **交付物**：artifact materializer
- **验收条件**：基线+diff 可还原；失败 diff 不污染父代
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-09 · 实现最小 ParentSelector（best/tournament/random）

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S4-01,S4-04
- **参考工作量**：5h
- **可并行**：可
- **交付物**：engine/selection.py
- **验收条件**：三种策略可配置并有确定性 seed
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-10 · 实现基础 Mutation Registry 占位

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S4-08
- **参考工作量**：4h
- **可并行**：可
- **交付物**：engine/mutation.py
- **验收条件**：支持 full rewrite/diff 两类操作注册
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-11 · 串联 Sandbox 与 TaskEvaluator

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S2-12,S3-06,S4-07
- **参考工作量**：8h
- **可并行**：否
- **交付物**：evaluation orchestrator
- **验收条件**：执行路径只有 Sandbox；状态与 Artifact 一致提交
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-12 · 实现 best/elite archive 最小逻辑

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S4-01,S3-08
- **参考工作量**：5h
- **可并行**：可
- **交付物**：candidate archive
- **验收条件**：正确性优先；同分时规则稳定；保留 provenance
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-13 · 实现 resume 与 orphan job recovery

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S4-05,S4-07
- **参考工作量**：7h
- **可并行**：否
- **交付物**：resume service
- **验收条件**：kill -9 后恢复租约；已提交结果不重跑
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-14 · 实现基础 GraphStore 与子图加载

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S4-02,S4-03
- **参考工作量**：6h
- **可并行**：可
- **交付物**：storage/graph_store.py
- **验收条件**：按 experiment/root/depth 加载；不跨 scope
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-15 · 实现 500 候选 soak 测试

- **Owner**：测试与质量
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S4-07~S4-13
- **参考工作量**：10h
- **可并行**：否
- **交付物**：tests/e2e/test_500_candidates.py
- **验收条件**：无状态丢失、孤儿 artifact、重复 committed run
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-16 · 实现 kill -9/进程崩溃故障注入

- **Owner**：测试与质量
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S4-13
- **参考工作量**：9h
- **可并行**：可
- **交付物**：fault injection suite
- **验收条件**：在生成/执行/解析/提交各阶段中断均可恢复
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S4-17 · 输出 Phase 1 审计报告

- **Owner**：文档与发布
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1~S4
- **参考工作量**：5h
- **可并行**：否
- **交付物**：reports/phase1_acceptance.md
- **验收条件**：逐条证明 Phase 1 验收项，记录残余问题
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。


## Sprint Definition of Done

- [ ] 所有关键路径任务 Done；非关键任务若延期必须更新 scope 与风险。
- [ ] 新增公共接口有 contract tests 和文档。
- [ ] 数据/状态变更有迁移或向后兼容策略。
- [ ] P0/P1 缺陷已关闭或在阶段门由用户接受。
- [ ] 交付物可从干净环境复现。
- [ ] 更新 `03_WBS总表.md` 状态、`07_风险登记册.md` 和 `10_需求追溯矩阵.md`。

## Sprint Review 重点

1. 是否真正满足设计边界，而非仅有演示路径。
2. 是否引入新的隐式全局状态、越权执行或双重真相源。
3. 是否可以故障恢复、重放和审计。
4. 是否需要砍 scope，而不是把风险推迟到发布前。
