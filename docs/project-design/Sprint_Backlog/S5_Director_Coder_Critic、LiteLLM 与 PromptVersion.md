# S5 Sprint Backlog：Director/Coder/Critic、LiteLLM 与 PromptVersion

> Phase：Phase 2  
> 推荐周期：5 个工作日  
> 阶段门：M3-A：Agent Loop Ready  
> 设计追溯：第三章 agents、第五章 5.3、第六章 Fast Loop、十三章依赖

## Sprint Goal

将 LLM 智能体接入可信候选闭环，并让每次调用、Prompt 与成本均可追踪和复现。

## 进入条件（Definition of Ready）

- 上游关键接口已冻结并有版本号。
- 所有 A 级任务明确 owner 和审查人。
- 依赖、测试夹具、目标平台和配置已准备。
- 不存在未处理的 P0 设计分歧。

## 任务卡

### S5-01 · 冻结 AgentContext/ThoughtOutput/CodeOutput

- **Owner**：架构负责人 / 高级模型
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S4-01,S4-07
- **参考工作量**：6h
- **可并行**：否
- **交付物**：agents/base.py contracts
- **验收条件**：结构化字段覆盖 lineage、memory、policy、prompt、model 与预算
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-02 · 实现 ModelGateway/LiteLLM Adapter

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-01
- **参考工作量**：7h
- **可并行**：否
- **交付物**：agents/model_gateway.py
- **验收条件**：统一 timeout/retry/usage；provider 异常可分类
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-03 · 实现 LLMCallLedger

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1-10,S5-02
- **参考工作量**：6h
- **可并行**：可
- **交付物**：调用账本
- **验收条件**：模型、role、prompt version、token、费用、延迟、响应 hash 可追踪
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-04 · 实现 PromptVersion Repository

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S1-10
- **参考工作量**：5h
- **可并行**：可
- **交付物**：prompt repository
- **验收条件**：角色+版本唯一；父版本、内容 hash、状态可记录
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-05 · 实现 ContextBuilder 与 token budget

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S5-01,S5-04
- **参考工作量**：7h
- **可并行**：否
- **交付物**：context builder
- **验收条件**：上下文分区明确；超预算按规则裁剪并记录
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-06 · 实现 DirectorAgent 最小版本

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-02,S5-05
- **参考工作量**：6h
- **可并行**：可
- **交付物**：agents/director.py
- **验收条件**：输出合法 ThoughtOutput；可引用父代和失败经验占位
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-07 · 实现 CoderAgent diff/full rewrite

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-02,S5-05
- **参考工作量**：7h
- **可并行**：可
- **交付物**：agents/coder.py
- **验收条件**：输出结构化 patch；失败可重试；不直接执行代码
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-08 · 实现 CriticAgent 静态审查

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-02,S5-05
- **参考工作量**：6h
- **可并行**：可
- **交付物**：agents/critic.py
- **验收条件**：检查语法、diff、需求一致性和明显越权；输出理由
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-09 · 实现结构化输出校验与 repair

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-06~S5-08
- **参考工作量**：5h
- **可并行**：可
- **交付物**：response parser
- **验收条件**：JSON/schema 失败可有限修复；原始响应保留 Artifact
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-10 · 实现 Agent retry/backoff/fallback

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-02,S5-09
- **参考工作量**：5h
- **可并行**：可
- **交付物**：retry policy
- **验收条件**：重试受预算约束；不可无限循环；provider fallback 可审计
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-11 · 实现基础静态模型路由占位

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S5-02
- **参考工作量**：3h
- **可并行**：可
- **交付物**：role→model mapping
- **验收条件**：Director/Coder/Critic 可独立配置模型
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-12 · 实现 token/费用预算硬门

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S5-03,S5-10
- **参考工作量**：5h
- **可并行**：可
- **交付物**：budget guard
- **验收条件**：预算不足时安全停止并保存状态
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-13 · 接入 Scheduler 生成链路

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S4-07,S5-06~S5-12
- **参考工作量**：8h
- **可并行**：否
- **交付物**：agent orchestration
- **验收条件**：Thought→Code→Critic→Candidate→Evaluation 全链路运行
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-14 · 使用 FakeLLM 编写确定性单测

- **Owner**：测试与质量
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S5-06~S5-10
- **参考工作量**：7h
- **可并行**：可
- **交付物**：tests/agents/
- **验收条件**：无需真实 API；覆盖非法输出、重试和预算耗尽
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-15 · 实现真实模型 smoke test（可选密钥）

- **Owner**：测试与质量
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S5-13
- **参考工作量**：4h
- **可并行**：可
- **交付物**：integration smoke
- **验收条件**：密钥缺失自动 skip；不写入日志；成本有上限
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S5-16 · 编写 Prompt 与 Agent 开发指南

- **Owner**：文档与发布
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S5-01~S5-15
- **参考工作量**：4h
- **可并行**：可
- **交付物**：docs/agents.md
- **验收条件**：含版本化、上下文安全、输出协议与调试方式
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。


## Sprint Definition of Done

- [ ] 所有关键路径任务 Done；非关键任务若延期必须更新 scope 与风险。
- [ ] 新增公共接口有 contract tests 和文档。
- [ ] 数据/状态变更有迁移或向后兼容策略。
- [ ] P0/P1 缺陷已关闭或在阶段门由用户接受。
- [ ] 交付物可从干净环境复现。
- [ ] 更新 `03_WBS总表.md` 状态、`07_风险登记册.md` 和 `10_需求追溯矩阵.md`。

## Sprint Review 重点

1. 是否真正满足设计边界，而非仅有演示路径。
2. 是否引入新的隐式全局状态、越权执行或双重真相源。
3. 是否可以故障恢复、重放和审计。
4. 是否需要砍 scope，而不是把风险推迟到发布前。
