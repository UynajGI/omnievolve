# S9 Sprint Backlog：SearchPolicyGenome、Policy Archive、L0 自适应、CLI 与审计发布

> Phase：Phase 3  
> 推荐周期：7 个工作日  
> 阶段门：M5：OmniEvolve v0.2 Alpha  
> 设计追溯：第五章 5.4.1、第六章 6.2/6.3、第十/十一/十二/十三章

## Sprint Goal

把搜索策略提升为可版本化、可导出、可回滚的一等对象，完成 v0.2 Alpha 的操作界面和审计闭环。

## 进入条件（Definition of Ready）

- 上游关键接口已冻结并有版本号。
- 所有 A 级任务明确 owner 和审查人。
- 依赖、测试夹具、目标平台和配置已准备。
- 不存在未处理的 P0 设计分歧。

## 任务卡

### S9-01 · 冻结 SearchPolicyGenome schema

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S8-09
- **参考工作量**：7h
- **可并行**：否
- **交付物**：meta/policy_genome.py
- **验收条件**：覆盖 selector、mutation mix、retrieval、novelty、routing、prompt、temperature
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-02 · 实现 SearchPolicyVersion Repository

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S1-10,S9-01
- **参考工作量**：6h
- **可并行**：可
- **交付物**：policy repository
- **验收条件**：版本不可变；父版本、状态、hash、配置可追踪
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-03 · 实现 Policy Archive Champion/Challenger

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S9-02
- **参考工作量**：7h
- **可并行**：否
- **交付物**：meta/policy_archive.py
- **验收条件**：每实验只有一个 active champion；challenger 独立
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-04 · 实现 L0 风险动作白名单

- **Owner**：安全与沙箱
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S8-10,S9-01
- **参考工作量**：6h
- **可并行**：可
- **交付物**：meta/governance.py
- **验收条件**：只允许温度、采样、检索预算、mutation mix、路由等
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-05 · 实现 L1/L2 拒绝与审计门禁

- **Owner**：安全与沙箱
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S9-04
- **参考工作量**：6h
- **可并行**：可
- **交付物**：governance rules
- **验收条件**：Prompt/控制器需 canary；评分/测试/hidden data 永久禁止
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-06 · 实现 L0 Policy Mutator

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S9-03,S9-04
- **参考工作量**：7h
- **可并行**：否
- **交付物**：policy mutator
- **验收条件**：根据 HealthOutput 生成有限、可验证的策略 diff
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-07 · 实现策略应用与原子回滚

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S9-02,S9-06
- **参考工作量**：8h
- **可并行**：否
- **交付物**：policy deployment
- **验收条件**：失败可恢复前一 champion；运行中任务绑定原策略版本
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-08 · 实现最小同预算 challenger 比较

- **Owner**：评估与实验
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S8-03,S9-03
- **参考工作量**：8h
- **可并行**：可
- **交付物**：policy experiment lite
- **验收条件**：至少支持离线窗口重放或短 canary，不做完整 L1
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-09 · 实现 CLI run/resume/status/best

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S4-13,S8-15,S9-07
- **参考工作量**：8h
- **可并行**：可
- **交付物**：cli.py commands
- **验收条件**：命令可用、错误码稳定、输出可脚本化
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-10 · 实现 CLI export/audit/doctor

- **Owner**：核心工程实现
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S2-14,S4-14,S8-15
- **参考工作量**：8h
- **可并行**：可
- **交付物**：graphml/json/md audit
- **验收条件**：可导出图、策略、环境、评估与 provenance
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-11 · 实现配置快照、校验与秘密遮蔽

- **Owner**：核心工程实现
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S5-12,S9-09
- **参考工作量**：6h
- **可并行**：可
- **交付物**：config snapshot
- **验收条件**：可复现配置入库；secret 不落盘/不导出
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-12 · 实现 Champion 完整导出/导入

- **Owner**：搜索、记忆与元进化
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S9-03,S9-10
- **参考工作量**：6h
- **可并行**：可
- **交付物**：policy bundle
- **验收条件**：策略、Prompt 引用、模型映射和 hash 可移植
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-13 · 实现端到端审计报告生成

- **Owner**：文档与发布
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S9-10,S9-12
- **参考工作量**：7h
- **可并行**：可
- **交付物**：audit report
- **验收条件**：从最佳候选追溯所有父代、LLM、Prompt、环境、评估和策略
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-14 · 实现 v0.2 Alpha E2E 基准任务

- **Owner**：测试与质量
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S1~S9
- **参考工作量**：10h
- **可并行**：否
- **交付物**：examples/e2e_alpha
- **验收条件**：完整跑通 ≥100 候选、恢复、检索、健康、自适应和导出
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-15 · 执行 Phase 1 全量 500 候选回归

- **Owner**：测试与质量
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S9-14
- **参考工作量**：10h
- **可并行**：可
- **交付物**：release soak
- **验收条件**：新模块未破坏恢复、安全和幂等性
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-16 · 完成 pyproject extras 与安装矩阵

- **Owner**：文档与发布
- **建议模型等级**：B（标准代码模型，可独立实现并由高级模型审查）
- **依赖**：S2,S6,S9-09
- **参考工作量**：6h
- **可并行**：可
- **交付物**：core/local/full extras
- **验收条件**：Linux 主路径通过；无 Docker/无 zvec 场景诊断正确
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-17 · 完成用户指南、架构图和发布说明

- **Owner**：文档与发布
- **建议模型等级**：C（轻量代码模型，适合局部实现、测试和文档）
- **依赖**：S9-09~S9-16
- **参考工作量**：7h
- **可并行**：可
- **交付物**：docs + CHANGELOG
- **验收条件**：从安装到首次运行、恢复、审计、限制均可操作
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。
### S9-18 · 召开 v0.2 Alpha Go/No-Go

- **Owner**：架构负责人 / 高级模型
- **建议模型等级**：A（高级模型主导，必须做接口/安全/数据一致性复核）
- **依赖**：S9-14~S9-17
- **参考工作量**：4h
- **可并行**：否
- **交付物**：release decision record
- **验收条件**：所有 P0 关闭；P1 有明确 owner/date；scope 未偷偷扩张
- **完成证据**：代码/文档路径、对应测试名、测试输出或审计截图/报告。
- **投喂边界**：只允许修改本任务直接涉及的模块；涉及 schema、Protocol、安全策略或公共数据类时必须先返回变更提案，不得直接扩展。


## Sprint Definition of Done

- [ ] 所有关键路径任务 Done；非关键任务若延期必须更新 scope 与风险。
- [ ] 新增公共接口有 contract tests 和文档。
- [ ] 数据/状态变更有迁移或向后兼容策略。
- [ ] P0/P1 缺陷已关闭或在阶段门由用户接受。
- [ ] 交付物可从干净环境复现。
- [ ] 更新 `03_WBS总表.md` 状态、`07_风险登记册.md` 和 `10_需求追溯矩阵.md`。

## Sprint Review 重点

1. 是否真正满足设计边界，而非仅有演示路径。
2. 是否引入新的隐式全局状态、越权执行或双重真相源。
3. 是否可以故障恢复、重放和审计。
4. 是否需要砍 scope，而不是把风险推迟到发布前。
